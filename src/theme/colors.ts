/**
 * src/theme/colors.ts — re-exports brand palette for in-app use.
 *
 * All color values in the app must come from here (or from brand/color/tokens.ts
 * directly). Never inline hex codes in components.
 */

export { palette } from '@brand/color/tokens';
export type { PaletteToken } from '@brand/color/tokens';
