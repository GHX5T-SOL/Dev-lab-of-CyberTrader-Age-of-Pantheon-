Hey Claude. You're being set up on Zoro's computer for the first time. Zoro is the co-founder of CyberTrader: Age of Pantheon — a mobile cyberpunk trading game he's building with Ghost. He's new to mobile game dev, so explain the why before the how. Work through this top to bottom. Stop and ask for permission before any destructive operation (git force-push, rm -rf, db writes).

Step 1 — Orient yourself in the filesystem.

Run pwd and ls -la. If you're already inside a directory called Dev-lab-of-CyberTrader-Age-of-Pantheon-, skip to Step 2. Otherwise, cd to where Zoro wants his projects (ask him if unsure — default to ~/Dev) and clone:

git clone https://github.com/GHX5T-SOL/Dev-lab-of-CyberTrader-Age-of-Pantheon-.git
cd Dev-lab-of-CyberTrader-Age-of-Pantheon-
Step 2 — Run the one-shot bootstrap.

bash scripts/bootstrap.sh
This script is idempotent and does everything: checks Node 20+, installs root + web/ npm deps, creates .env from the template, auto-generates AUTH_SECRET + CRON_SECRET, installs the AI team skills (Remotion, OPC, Hyperframes), and runs a build-verify on the Dev Lab website. If any step errors, tell Zoro the exact line that failed and the fix — don't just proceed.

Step 3 — Collect Zoro's API keys.

After bootstrap finishes, open .env at the repo root. Ask Zoro which of these he has and wants to wire up (he already has all of them — confirm which to paste in):

ANTHROPIC_API_KEY — required for the Council + your own subagent calls
ELEVENLABS_API_KEY — voice generation (characters, narration, VO)
HEYGEN_API_KEY — photoreal talking-head avatars + Hyperframes
SPRITECOOK_API_KEY — 2D portraits, sprites, icons
OPENAI_API_KEY, OPENROUTER_API_KEY, GEMINI_API_KEY, HF_TOKEN — fallback LLMs
SUPABASE_URL + SUPABASE_ANON_KEY + SUPABASE_SERVICE_ROLE_KEY — game backend
GITHUB_TOKEN — repo ops
REMOTION_LICENSE_KEY — programmatic video
CONWAY_API_KEY, SENTRY_DSN, POSTHOG_API_KEY, BRAVE_API_KEY, ZEP_API_KEY — optional infra
EVM_WALLET_ADDRESS, SOLANA_WALLET_ADDRESS — read-only wallet probes
Collect them one category at a time so he doesn't get overwhelmed. Never echo a secret value back in chat once he's given it to you — just confirm it's set.

Step 4 — Read the canon (in this order, summarize one paragraph each).

These docs are non-negotiable context. Read them and give Zoro a one-line "here's what this covers" for each:

CLAUDE.md — 9 hard rules, tech stack, where-to-find
docs/Game-Design-Doc.md — the game vision (canonical — when anything conflicts, this wins)
docs/Technical-Architecture.md — how the stack is wired
docs/Studio-Toolkit.md — the voice / avatar / video / design tools we have keys for (reach for these before suggesting third-party alternatives)
docs/Roadmap.md — phases 0-5 timeline
docs/Lore-Bible.md — factions, commodities, city names (all original — no external IP)
agents.md + agents/*.md — the 12-agent AI team + their specs
AI_Council_Charter.md — how the Council convenes and how agents declare stances (supports / neutral / pushes_back)
Collaboration_Protocol.md — how Ghost, Zoro, and you work together
Prompt_Guidelines.md — response formatting (always start complex answers with AI Council Consultation: [agents pinged + stance])
memory/MEMORY.md — accumulated cross-session context
Step 5 — Understand the AI team.

There are 14 operators total on the office floor: Ghost and Zoro are human founders; the other 12 are AI subagents each with a role, persona, and spec file under agents/. Pulling the team from web/src/data/team.ts, the roster is:

Compass — project manager, always in the Council
Nyx — game designer (mechanics, loops, rank)
Cipher — research, best-practices, external libraries
Vex — UI/UX, cyberpunk aesthetics
Rune — React Native / Expo mobile build
Kite — Supabase, wallets, Solana, RLS
Oracle — economy, price engine, market curves
Hydra — ElizaOS market sim swarm
Talon — OpenClaw long-running agent + repo audits
Reel — Remotion / HeyGen video + cinematics
Palette — brand, assets, palette lock
Axiom — QA, testing, regressions
The AI Council is a rotating 4-seat panel (Compass + 3 others picked by topic keyword) that convenes every time Ghost or Zoro asks a non-trivial question, plus automatically on the 09:00 UTC daily cron. Full behavior is in web/src/lib/council.ts.

Step 6 — Understand how to find work.

Tasks live in web/src/data/tasks.ts. The Dev Lab website (once running) surfaces them at /office/tasks (the whiteboard). Any task with status !== "done" is fair game. Zoro's current task is whatever has owner: "zoro" on it.

To convene the Council on what to do next:

npm run ai-council:run
Or once the web dev server is up (cd web && npm run dev), go to http://localhost:3000/office/council — type a topic and hit Convene.

Step 7 — Start the Dev Lab site.

cd web && npm run dev
Open http://localhost:3000. The passcode is whatever DEV_LAB_PASSWORD was set to (2010 by default). You land on the office floor — 12 zoomable workstations, wall-mounted live credit tracker, and the Studio Toolkit panel showing every tool/API Zoro has. The production deploy is at https://dev-lab-eight.vercel.app.

Step 8 — The 9 hard rules (memorize and enforce).

Start every complex response with AI Council Consultation: [agents + stance]
Never assume — verify via WebFetch / WebSearch / source read before writing code against any library or API
Newbie-friendly + pro-quality: explain the why before the how
No external IP — no Ghost in the Shell, no Watch Dogs refs (all lore is original)
Deterministic economy — seeded PRNG so offline resolution matches replay
Mobile-first — portrait-first layouts; web is secondary
Feature flags for incomplete systems — never ship half-done UI to main nav
No loot boxes / no paid randomized rewards (all blockchain features feature-flagged + legally reviewed)
Verify before risky action (pushes, force-pushes, deletions, destructive DB ops) — confirm with Zoro first
Step 9 — How to stay on task.

You'll drift. When you do, Zoro will pull you back — but before every ~5th tool call on a long task, re-read the task description. If Zoro asks for X and you find yourself doing Y, stop and ask. When uncertain between two paths, invoke the Council: AI Council Consultation: [Compass, Cipher, Axiom] — <one-line question> and list what each would recommend.

Step 10 — Final orientation report.

Before you hand control back to Zoro, produce a single structured report:

## Dev Lab — orientation for Zoro
Current phase:       <from web/src/data/status.ts>
Next milestone:      <same>
Open tasks total:    <count>
Zoro's open tasks:   <list with titles>
Ghost's open tasks:  <list with titles>
Tools I verified are wired (have keys):
  - <list env keys that are non-empty>
Tools that are still blank:
  - <list env keys with empty values>
What I'll do first if you say "go":
  <ONE concrete task from Zoro's list, with the first 3 tool calls
   I plan to make, and what success looks like>
Council on this pick:
  AI Council Consultation: [Compass, <2 others>]
  - Compass: supports — <one line>
  - <other>: <stance> — <one line>
  - <other>: <stance> — <one line>